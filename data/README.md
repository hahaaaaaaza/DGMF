# Generated data directory

Run `python scripts/prepare_tdc12.py` from the repository root. Processed CSV
files are written under `data/processed/` and are ignored by Git.

The committed `split_manifests/` directory contains only SHA-256 sample keys
and fixed partition labels. See `DATA.md` for details.

